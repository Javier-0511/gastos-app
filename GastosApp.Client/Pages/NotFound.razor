@page "/not-found"
@layout MainLayout

<div class="notfound stagger">
    <div class="notfound-code">404</div>
    <h3 class="notfound-title">Página no encontrada</h3>
    <p class="text-muted">La página que buscas no existe o se ha movido.</p>
    <a href="" class="btn btn-primary mt-2">Volver al inicio</a>
</div>

<style>
    .notfound {
        max-width: 420px;
        margin: 0 auto;
        text-align: center;
        padding-top: 12vh;
    }
    .notfound-code {
        font-size: 4.5rem;
        font-weight: 800;
        line-height: 1;
        letter-spacing: -0.04em;
        background-image: var(--accent-gradient);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
    }
    .notfound-title {
        font-weight: 800;
        letter-spacing: -0.02em;
        margin: 12px 0 4px;
    }
</style>
